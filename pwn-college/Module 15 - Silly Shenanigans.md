In Linux, whenever a Shell starts up, it looks for the .bashrc file in your home directory and executes it as a startup script. One can customize this .bashrc file. 

In this module, there wasn't anything really to "learn" it was mostly just applying the concepts we've learnt in the previous modules and just finding ways to print the flag again. 

It mostly involved targeting the vulnerabilities left by the other user, and using them to our advantage to get the flag.